export interface Day {
  id: string;
  title: string;
}
export interface Style {
  borderColor: string;
  background: string;
  foreground: string;
  fontColor: string;
}

export interface Time {
  id: string;
  subject: string;
  instructor: string;
  place: string;
}
